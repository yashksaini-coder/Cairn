[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
# Added so `cargo build --target` in CI can prove cairn-core is portable
# without a Solana toolchain present (KPI E4).
targets = ["x86_64-unknown-linux-gnu"]
